Amazon S3 (Simple storage service)

> We can create buckets and store objects in them.
> Buckets are like logical container and every object inside bucket is defined by its key.
> S3 can store both structured, unstructured data
